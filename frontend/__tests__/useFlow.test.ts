/**
 * @deprecated
 * This file tested the legacy imperative useFlow(token) API.
 * The hook has been migrated to an SWR-based hook in src/hooks/useFlow.ts.
 * All active tests now live in src/__tests__/hooks/useFlow.test.ts.
 */

describe("useFlow (legacy stub)", () => {
  it("is a stub — see src/__tests__/hooks/useFlow.test.ts", () => {
    expect(true).toBe(true);
  });
});
